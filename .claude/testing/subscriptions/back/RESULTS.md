# Resultados - Modulo Subscriptions (back)

| Fecha | Caso | Resultado | Bug | Commit fix |
|-------|------|-----------|-----|------------|
| 2026-09-02 | CU-01 login | OK | Nota: el JWT viene por cookie `session_token` (httpOnly), `data.token` del body queda vacio a proposito. CU-01.md asumia `data.token` -> se corrige la doc | - |
| 2026-09-02 | CU-02 catalogo planes/modulos | OK | - | - |
| 2026-09-02 | CU-03 suscripcion actual | OK | Confirma en vivo que `GET /me` ya embebe `subscription_type` (fix del boton Pagar/Extender) | 8c3a0356 |
| 2026-09-02 | CU-04 compra suscripcion | OK (con fix) | Saldo insuficiente: 402, nada se toca. Saldo suficiente: descuenta exacto `price*months`, crea fila `business_subscriptions` con fechas correctas, `business_id` inyectado en el body se ignora. BUG encontrado: `CreateSubscriptionAndActivate` resuelve `payment_method` vacio -> `WALLET` solo para la fila en DB, nunca lo escribe de vuelta en el `*entities.BusinessSubscription` que se retorna, asi que la respuesta del POST mostraba `payment_method:""` aunque en BD si quedaba `WALLET` correctamente | pendiente commit |
| 2026-09-02 | CU-05 super admin CRUD (pago, editar fechas, deshabilitar) | OK (con fix CRITICO) | register-payment y edit-dates OK, 403 correcto para DEMO_TOKEN. **BUG GRAVE encontrado en 5.4**: al deshabilitar (`disable`), `HasModuleAccess` (`has_module_access.go:28-30`) trataba `subTypeID==nil` como "sin restriccion, todo abierto" -- una cuenta deshabilitada/expirada terminaba con MAS modulos accesibles (10 de 13, solo bloqueaba storefront/tickets/delivery) que un negocio con el plan mas barato. Confirmado que en PRODUCCION hay un negocio real (`Alejandra`, business_id 38, activo desde 2026-04-13) que nunca tuvo plan asignado y depende de este comportamiento para funcionar. El usuario indico que ese negocio no es prioridad, se puede desplegar igual. Fix: `HasModuleAccess` ahora distingue "sin plan pero activo" (se mantiene el fallback abierto, es intencional y esta testeado) de "sin plan y no activo" (deniega), usando `GetBusinessSubscriptionMeta`. Test nuevo: `TestHasModuleAccess_SinPlanYCuentaDeshabilitada_TodoQuedaCerrado` | pendiente commit |
| 2026-09-02 | CU-06 custom plans CRUD + ownership | OK (con fix CRITICO) | 6.1-6.3 y 6.5 OK. Nota: `PUT /custom-plans/{id}` hace reemplazo TOTAL del objeto (no merge parcial) — si el caller omite `active`/`description` quedan sobreescritos a `false`/`""`. No es un bug per se (es semantica PUT estandar) pero es una trampa real si algun consumidor de API manda solo el campo que quiere cambiar; el frontend debe reenviar siempre el objeto completo. **BUG CRITICO encontrado**: `CreateCustomPlan` (`create_custom_plan.go`) nunca seteaba `Payable` en la entidad Y ADEMAS el repo `CreateSubscriptionType` (`subscription_type_repository.go`) nunca lo mapeaba al INSERT -- todo plan personalizado nuevo nacia con `payable=false` y jamas podia ser auto-comprado/renovado por el propio negocio via `POST /purchase` (fallaba con "este plan no se puede comprar, se asigna automaticamente"), incluso despues del fix del boton Pagar/Extender de hoy. Los 3 custom plans que ya existen en produccion (ids 4, 5, 8 — incluido el de negocio 34 "sin intermediarios") tienen `payable=true` porque alguien los parcheo a mano en algun momento, pero cualquier plan personalizado nuevo creado desde entonces (o futuro) hereda el bug. Fix: `Payable: true` explicito en `create_custom_plan.go` y mapeo agregado en el repo. Confirmado en vivo: se creo un plan nuevo, quedo `payable=true` en DB, Demo pudo comprarlo, y el check de ownership (6.4b, con un segundo negocio de prueba creado localmente) rechazo correctamente a un negocio ajeno con "subscription type not found" | pendiente commit |
| 2026-09-02 | CU-07 overrides de modulos | OK (con fix relacionado) | Grant/list/revoke OK, `granted_by_user_id` correcto, 403 para DEMO_TOKEN en las 3 rutas, override sobre modulo restringido-por-defecto (storefront) funciona y se revoca correctamente. **Efecto colateral descubierto durante la prueba**: al comprar un custom plan de prueba para Demo (CU-06) y luego borrarlo, `business.subscription_type_id` quedo apuntando a un plan soft-deleted; `HasModuleAccess` tenia una TERCERA instancia del mismo patron "referencia invalida -> permitir todo" en `subType == nil` (plan borrado/inexistente), ademas de la de CU-05. Fix: extraido a un helper compartido `allowWhenNoPlan` que aplica la misma regla (activo -> fallback abierto histórico, no activo -> cerrado) en ambos casos. Test nuevo: `TestHasModuleAccess_PlanBorradoYCuentaDeshabilitada_TodoQuedaCerrado`. Nota operativa: borrar un custom plan mientras un negocio esta activo en el no lo reasigna a ningun otro plan, lo deja con una referencia colgante — no se corrigio (fuera de alcance de este fix puntual), pendiente evaluar si `DeleteCustomPlan` deberia impedirlo o reasignar automaticamente | pendiente commit |
| 2026-09-02 | CU-08 dias de cortesia (`courtesy_until`) | OK | 8.1: extender cortesia con cuenta activa NO toca `business.subscription_end_date` ni `business_subscriptions.end_date`, solo `subscription_courtesy_until = end_date + N dias`, auditoria `courtesy_extended` correcta. 8.2: con cuenta `expired`, el mismo endpoint reactiva automaticamente (`subscription_status -> active`), `my-modules` vuelve a mostrar los modulos del plan sin relogin (dentro de la misma sesion de API, sin pasar por JWT), auditoria `subscription_reactivated` adicional. 8.3 (dias=0 -> 400) y 8.4 (403 para negocio normal) OK. Nota: el endpoint real es `POST /extend-days`, no `/extend-courtesy` como decia el nombre interno — la doc CU-08 quedo con el path correcto | - |
| 2026-09-02 | CU-09 dia de corte fijo (`cutoff_day`) | OK | `edit-dates` con `cutoff_day` lo persiste y lo audita ("dia de corte: N"). Reactivacion automatica tambien funciona desde `edit-dates` (no solo desde `extend-days`): con cuenta `expired`, editar fechas con `end_date` futuro reactiva y deja auditoria `subscription_reactivated` | - |
| 2026-09-02 | CU-10 reactivar/revertir pago/historial/auditoria | OK | disable+reactivate, revert-payment, historial de pagos y audit-logs todos funcionan y estan protegidos con 403 para negocio normal. Nota (no bug, inconsistencia de API menor): `disable` recibe `business_id` por query param, `reactivate` lo recibe en el body JSON — vale la pena unificar en algun momento pero no rompe nada | - |
| 2026-09-02 | CU-12 excedentes al renovar | OK (via unit tests) | La logica de `ComputeOverageAmount` ya esta cubierta por tests unitarios que pasan (`subscriptions_test.go`). No se armo el escenario E2E completo (requiere >50 envios en el ciclo de un negocio con plan `free`) por costo/beneficio — el unico plan con overage configurado localmente es `free` (50 envios incluidos, $1000 c/u) | - |
| 2026-09-02 | CU-13 KPIs y listado admin de negocios | OK | `GET /admin/kpis` y `GET /admin/businesses` (paginado) devuelven datos coherentes con el estado real de Demo, 403 correcto para negocio normal | - |
