name: Backend CI/CD - Probability

on:
  push:
    branches:
      - main
    paths:
      - 'back/central/**'
      - 'back/migration/**'
      - '.github/workflows/backend-ci-cd.yml'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: 476702565908
  IMAGE_NAME: backend

jobs:
  build-and-push:
    name: Build and Push to ECR
    runs-on: ubuntu-24.04-arm  # Runner ARM64 nativo - más rápido y barato

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: back/central/go.sum

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get install -y podman

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        run: |
          aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
            podman login --username AWS --password-stdin ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com

      - name: Build image with Podman (ARM64)
        working-directory: ./back
        run: |
          podman build \
            --platform linux/arm64 \
            -f central/docker/Dockerfile \
            -t ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/probability-backend:latest \
            .

      - name: Push image to ECR
        run: |
          podman push ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/probability-backend:latest

  deploy:
    name: Deploy to Server
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      - name: Upload compose file
        run: |
          scp -o StrictHostKeyChecking=no \
            infra/compose-prod/podman-compose.yaml \
            ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:~/probability/infra/compose-prod/

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            set -e
            export PATH=$PATH:$HOME/.local/bin:/usr/local/bin:/usr/bin

            # Crear directorio si no existe
            mkdir -p ~/probability/infra/compose-prod

            # Instalar/reinstalar AWS CLI para ARM64
            # Verificar si AWS CLI está instalado y funciona correctamente
            if ! command -v aws &> /dev/null || ! aws --version &> /dev/null; then
              echo "📦 Instalando/reinstalando AWS CLI para ARM64..."
              # Remover instalación anterior si existe
              sudo rm -rf /usr/local/aws-cli /usr/local/bin/aws /usr/local/bin/aws_completer
              # Descargar e instalar versión ARM64
              curl "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o "awscliv2.zip"
              unzip -q awscliv2.zip
              sudo ./aws/install --bin-dir /usr/local/bin --install-dir /usr/local/aws-cli
              rm -rf aws awscliv2.zip
              # Verificar instalación
              /usr/local/bin/aws --version || {
                echo "❌ Error: AWS CLI no se instaló correctamente"
                exit 1
              }
              echo "✅ AWS CLI instalado correctamente"
            else
              echo "✅ AWS CLI ya está instalado y funcionando"
            fi

            cd ~/probability/infra/compose-prod || exit 1

            # Asegurar que AWS CLI esté en PATH
            export PATH="/usr/local/bin:$PATH"

            # Login a ECR privado
            echo "🔐 Logging in to ECR..."
            /usr/local/bin/aws ecr get-login-password --region us-east-1 | \
              podman login --username AWS --password-stdin 476702565908.dkr.ecr.us-east-1.amazonaws.com

            # Obtener el ID de la imagen actual antes del pull
            echo "📦 Pulling latest image from ECR..."
            OLD_IMAGE_ID=$(podman images --format "{{.ID}}" 476702565908.dkr.ecr.us-east-1.amazonaws.com/probability-backend:latest 2>/dev/null || echo "")
            
            # Pull latest image (sin caché para asegurar que descarga la nueva)
            podman pull --quiet 476702565908.dkr.ecr.us-east-1.amazonaws.com/probability-backend:latest
            
            # Obtener el ID de la nueva imagen
            NEW_IMAGE_ID=$(podman images --format "{{.ID}}" 476702565908.dkr.ecr.us-east-1.amazonaws.com/probability-backend:latest)
            
            echo "📊 Old image ID: ${OLD_IMAGE_ID:-none}"
            echo "📊 New image ID: ${NEW_IMAGE_ID}"
            
            # Verificar si la imagen realmente cambió
            if [ "$OLD_IMAGE_ID" != "$NEW_IMAGE_ID" ] || [ -z "$OLD_IMAGE_ID" ]; then
              echo "✅ Nueva imagen detectada, recreando contenedor..."
              
              # Recrear solo este servicio sin afectar dependencias (--no-deps)
              # Usamos pull primero para asegurar que compose use la nueva imagen
              podman-compose -f podman-compose.yaml pull back-central
              
              # Recrear el contenedor SIN reiniciar dependencias (reemplazo sutil)
              echo "🔄 Recreando contenedor con nueva imagen (sin afectar dependencias)..."
              podman-compose -f podman-compose.yaml up -d --force-recreate --no-deps back-central
              
              # Reiniciar nginx completamente para que resuelva las nuevas IPs de los contenedores
              # nginx -s reload no es suficiente porque mantiene en caché las IPs de los upstreams
              echo "🔄 Reiniciando nginx para reconectar con el nuevo contenedor..."
              podman restart nginx_prod 2>/dev/null || echo "ℹ️  Nginx no está corriendo"
              sleep 5  # Esperar a que nginx se reinicie
              
              echo "⏳ Esperando a que el contenedor inicie (20 segundos)..."
              sleep 20
              
              # Verificar que el contenedor está corriendo (con reintentos)
              MAX_RETRIES=3
              RETRY_COUNT=0
              CONTAINER_STATUS=""
              
              while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
                CONTAINER_STATUS=$(podman inspect backend_prod --format '{{.State.Status}}' 2>/dev/null || echo "not_found")
                echo "📋 Intento $((RETRY_COUNT + 1))/$MAX_RETRIES - Estado del contenedor: $CONTAINER_STATUS"
                
                if [ "$CONTAINER_STATUS" = "running" ]; then
                  break
                fi
                
                RETRY_COUNT=$((RETRY_COUNT + 1))
                if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                  echo "⏳ Esperando 5 segundos más..."
                  sleep 5
                fi
              done
              
              # Mostrar logs antes de verificar estado final
              echo "📋 Últimos logs del contenedor:"
              podman logs --tail 30 backend_prod 2>/dev/null || true
              
              # Verificar estado final
              CONTAINER_STATUS=$(podman inspect backend_prod --format '{{.State.Status}}' 2>/dev/null || echo "not_found")
              if [ "$CONTAINER_STATUS" != "running" ]; then
                echo "❌ El contenedor no está corriendo después de $MAX_RETRIES intentos"
                echo "📋 Estado final: $CONTAINER_STATUS"
                echo "📋 Revisando logs de error:"
                podman logs --tail 50 backend_prod 2>/dev/null || true
                
                # Intentar reiniciar manualmente
                echo "🔄 Intentando reiniciar manualmente..."
                podman restart backend_prod 2>/dev/null || podman start backend_prod 2>/dev/null || true
                sleep 10
                
                # Verificar una vez más
                CONTAINER_STATUS=$(podman inspect backend_prod --format '{{.State.Status}}' 2>/dev/null || echo "not_found")
                if [ "$CONTAINER_STATUS" != "running" ]; then
                  echo "❌ Error crítico: No se pudo iniciar el contenedor. Revisar logs manualmente."
                  exit 1
                else
                  echo "✅ Contenedor iniciado después de reinicio manual"
                fi
              else
                echo "✅ Contenedor corriendo correctamente"
              fi
              
              # Verificar que el contenedor está usando la nueva imagen
              CONTAINER_IMAGE_ID=$(podman inspect backend_prod --format '{{.Image}}' 2>/dev/null || echo "")
              NEW_IMAGE_FULL_ID=$(podman images --format "{{.ID}}" --no-trunc 476702565908.dkr.ecr.us-east-1.amazonaws.com/probability-backend:latest | head -1)
              
              echo "📋 Image ID del contenedor: ${CONTAINER_IMAGE_ID:0:12}"
              echo "📋 Image ID esperada: ${NEW_IMAGE_FULL_ID:0:12}"
              
              if [ -n "$CONTAINER_IMAGE_ID" ] && [ -n "$NEW_IMAGE_FULL_ID" ]; then
                if [ "${CONTAINER_IMAGE_ID:0:12}" = "${NEW_IMAGE_FULL_ID:0:12}" ]; then
                  echo "✅ Contenedor actualizado correctamente con la nueva imagen"
                else
                  echo "⚠️  Advertencia: El contenedor podría no estar usando la imagen más reciente"
                fi
              fi
              
            else
              echo "ℹ️  La imagen no cambió (mismo ID), forzando recreación del contenedor (sin afectar dependencias)..."
              podman-compose -f podman-compose.yaml up -d --force-recreate --no-deps back-central
              sleep 15
              
              # Verificar estado
              CONTAINER_STATUS=$(podman inspect backend_prod --format '{{.State.Status}}' 2>/dev/null || echo "not_found")
              echo "📋 Estado después de recreación: $CONTAINER_STATUS"
              
              if [ "$CONTAINER_STATUS" != "running" ]; then
                echo "⚠️  El contenedor no está corriendo, intentando reiniciar..."
                podman restart backend_prod 2>/dev/null || podman start backend_prod 2>/dev/null || true
              fi
            fi

            # Clean up imágenes antiguas
            echo "🧹 Limpiando imágenes antiguas..."
            podman image prune -f

            echo "✅ Backend deployed successfully"
          EOF
